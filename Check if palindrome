#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	string s;
	cin>>s;
	int i= 0, j = s.length()-1;
	while(i++<j--)
	  if(s[i]!=s[j])
	    break;
	if(i<j)
	  cout<<"Not palindrome";
	else
	  cout<<"Palindrome";
	return 0;
}
